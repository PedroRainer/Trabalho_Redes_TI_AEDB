:80 {
    encode gzip
    log

    # site Next.js
    handle_path /* {
        reverse_proxy front:3000
    }

    # Sem /ws agora: tráfego entre front e views é TCP interno, via /api/tcp no Next
    header {
        X-Content-Type-Options "nosniff"
        X-Frame-Options "DENY"
        Referrer-Policy "strict-origin-when-cross-origin"
    }
}
